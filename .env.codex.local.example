# Copy the values you need into .env.local.
# Keep real secrets out of git.

# Linker reads the shared gmail-agent mail API from this origin in local development.
VITE_CODEX_MAIL_URL=http://127.0.0.1:4192
